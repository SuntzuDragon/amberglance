// Copy this file to `secrets.h` and fill in your network.
// `include/secrets.h` is gitignored — it must never be committed.
//
// Note: the ESP32-S3 radio is 2.4GHz only. A 5GHz-only SSID will not connect.
#pragma once

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"
